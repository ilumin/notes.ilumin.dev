---
title: บาป 9 ประการของการเขียน log ใน Java
tags:
  - programming
  - java
  - log
emoji: 😭
created: 2018-08-22
---

บัญญัติที่ 11 ใน [The Twelve-Factor App](https://12factor.net)
กล่าวว่า "เราควรมี log และ log นั้นควรเขียนเป็น stream" หมายถึงการเขียนข้อมูลเพิ่มต่อจากข้อมูลเดิมไปเรื่อยๆ เพื่อที่เราจะสามารถไล่ดูพฤติกรรมของ application ได้ง่าย

ในบางครั้งบางคราว ด้วยความไม่รู้ เรามักจะทำบาปเกี่ยวกับ log ดังนี้

## 1. log sensitive information

ยิ่งเรา log มากเท่าไหร่ เราก็รู้พฤติกรรมของ application มากขึ้นเท่านั้น แต่ในบางครั้งบางคราว ข้อมูลบางอย่างที่เรา log ลงไป อาจจะทำร้ายเราได้ในอนาคต เช่น user credentail (ข้อมูลที่ไม่ควรเปิดเผยของผู้ใช้งาน เช่น รหัสผ่าน) หรือ ข้อมูลทางการเงิน ฯลฯ

ถ้าหากว่าข้อมูลเหล่านี้รั่วไหลออกไปนอกระบบ นอกจากจะเกิดความเสียหายกับผู้ใช้แล้ว ก็อาจจะเกิดการฟ้องร้องกับบริษัทด้วย

ทางที่ดีที่สุดที่จะป้องกันได้คือ ไม่เก็บข้อมูลเหล่านี้ใน log ซึ่ง developer ควรทำความเข้าใจให้ดีว่าข้อมูลประเภทไหนจัดอยู่ใน sensitive information โดยสามารถศึกษาได้จาก [Logging Cheat Sheet (Data to exclude)](https://www.owasp.org/index.php/Logging_Cheat_Sheet#Data_to_exclude)

เพิ่มเติมจากข้อนี้คือ การเก็บ input ของผู้ใช้งานโดยที่ไม่ทำ decode

## 2. log มากเกินไป

เมื่อเก็บข้อมูลเยอะเกินไป แทนที่เราจะสามารถอ่านพฤติกรรมของ application ได้ง่ายๆ มันจะกลายเป็นอ่านยากขึ้น รวมทั้งไฟล์ log ก็จะใหญ่จนเกินไป มีปัญหาเรื่องพื้นที่ในการเก็บข้อมูลอีก

นอกเหนือจากนี้ ก็จะเป็นความลำบากในการเขียนโค้ด เนื่องจากจะมีโค้ดที่ไม่เกี่ยวข้องกับ business logic เยอะแยะมากมายไปหมด

ดังนั้นเราควรมีสติในการเขียน log โฟกัสให้ดีว่าเราอยากจะรู้พฤติกรรมอะไร ซึ่งจะดีมากๆถ้าเราออกแบบว่าจะ log อะไรเพื่อให้สอดคล้องกับสิ่งที่ business ต้องการตั้งแต่ก่อนจะเริ่ม sprint และเราสามารถใช้ AOP ในการเขียน log บางกรณี

## 3. log น้อยเกินไป

การเก็บข้อมูลน้อยเกินไปในกรณีนี้ไม่ได้หมายถึงการเขียน log ในปริมาณที่น้อย แต่หมายถึงการเขียน log ที่ไม่สื่อถึงพฤติกรรมต่างๆใน application เช่น

```
Operation failed.
```

แบบนี้เรารู้แค่มี process อะไรบางอย่างพัง ซึ่งอาจจะเป็นเว็บล่ม ทำงานต่อไม่ได้ ... ซึ่งเป็นพฤติกรรมที่มันบ่งบอกมาให้เห็นอยู่แล้ว ทางที่ดีเราควรจะ log เพิ่มเติมว่า failed เพราะอะไร หรือมี stack trace ก็ยังดี

```
File upload picture.jpg failed
```

แบบนี้มีข้อมูลมากกว่าแบบแรก แต่ก็ไม่ได้ช่วยอะไรอยู่ดี เพราะเราก็ไม่สามารถรู้ว่า ที่ระบบไม่สามารถ upload file ได้ มันเกิดจากอะไร

## 4. ไม่ทำ log rotate

การทำ log rotate หมายถึงการ backup log เป็นระยะๆ โดยปกติจะทำเป็นวัน เมื่อเก็บ log จนสิ้นวันก็จะทำการ backup เป็นไฟล์ของวันนั้นและเคลียร์ไฟล์ log เดิม

ซึ่งเป็น option ของ log4j อยู่แล้ว ไม่จำเป็นต้องไป reinvent the wheel ให้เสีย effort

## 5. ใช้ log level ผิด

log level หมายถึงระดับของความน่าสนใจใน log เช่น `info` ก็เป็นข้อมูล log ธรรมดาๆ, `warning` เป็น log ที่บอกว่ามีอะไรบางอย่างที่ผิดปกติ แต่ไม่ถึงระดับร้ายแรง หรือ `error` ที่บอกว่ามันเกิดปัญหาอะไร

การทำ log level ผิด บางครั้งอาจจะเกิดจากความไม่รู้ และไม่ใส่ใจ ซึ่งจะทำให้เราไม่สามารถเอา log ไปใช้ประโยนช์อย่างอื่นได้ เช่น ทำ alarm กรณีที่เกิด error rate มากกว่าปกติ ซึ่งอาจจะหมายถึงระบบกำลังรับไม่ไหว ก็เป็นได้

และที่ยากที่สุดคือ อาจจะเกิดจากความไม่โปร่งใส่ในทีม ที่พยายามจะเลี่ยงบาลีว่ามันไม่ใช่ error ที่ต้องรับผิดชอบ

## 6. พยายามจะ track operation ข้าม service

ปัญหาของการทำ log ใน micro service คือ เราจะรู้ได้อย่างไรว่า api call หรือ log ที่เกิดขึ้นใน service ต่างๆมันเกิดจาก request เดียวกันกับที่ user กำลังทำ action อะไรบางอย่างใน application ซึ่งจะทำให้เราไม่รู้ความสัมพันธ์ของ log mี่เกิดขึ้น

ผมเคยเห็นท่าพิสดารที่ dev พยายามจะใส่ข้อมูลอะไรบางอย่างลงใน API request เพื่อเอาไปสร้าง log และตอบคำถามด้านบน

การทำแบบนี้สามารถทำได้ ไม่ผิดอะไร แต่จริงๆมันมีเครื่องมือที่สร้างมาเพื่อแก้ปัญหาแบบนี้อยู่แล้ว (เช่น zipkin) ดังนั้นทางที่เหมาะสม ควรจะเอา effort ไปศึกษาและเอาเครื่องมือมาใช้ มากกว่าการสร้างเอง (อย่างน้อยจะได้รู้ปัญหาที่คนส่วนใหญ่เจอ และเอาไปใส่ใน CV ได้)

## 7. ไม่ได้ log เป็น JSON

เนื่องจากปัจจุบัน เรามีเครื่องมือที่ใช้ในการสร้างกราฟและดู log อย่าง Kibana การที่เราสร้าง log เป็น text ที่ไม่มี format จะทำให้เราใช้เครื่องมือพวกนี้ได้ไม่คุ้มค่าเท่าไหร่

เพราะ Kibana สามารถสร้าง index หรือ metric ต่างๆจาก log message ได้ และยิ่งถ้า log message เป็น JSON ก็จะทำให้สามารถสร้างได้ง่ายขึ้น เพราะ JSON มี pattern ที่ชัดเจนมากกว่า text ที่ไร้ format

## 8. log ที่มีผลกระทบกับ performance

ข้อนี้เป็นสิ่งที่สำคัญที่สุดเพราะ

- log ต้องไม่ block app
- log ต้องไว
- log ควรจะทำ trim กรณีที่ข้อมูลยาวเกินไป
- review log ก่อนที่จะไป production

---

ทั้งหมดนี้สรุปจากไหนก็ไม่รู้ จำไม่ได้
